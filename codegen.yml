overwrite: true
schema: 'https://idolypride-spotlight.hasura.app/v1/graphql'
documents: 'api/**/*.graphql'
generates:
  generated/graphql.ts:
    plugins:
      - typescript
      - typescript-operations
      - typed-document-node
  ./graphql.schema.json:
    plugins:
      - introspection
